import { Meta, Story, Canvas } from "@storybook/blocks";
import { Tooltip } from ".";
import { Text } from "../text";
import { FlexContainer, Flex } from "../container";
import { Button } from "../button";
import { SVGIcon } from "../svgicon";
import { Input } from "../input";
import * as TooltipStories from './Tooltip.stories';
import * as TooltipStories from './Tooltip_.stories';

<Meta of={TooltipStories} />

# Tooltip

#### Props

> *   `caption`: `ReactNode` — **required** (tooltip content)
> *   `position`: `left` | `right` — **optional** (default position is center)
> *   `bottom`: `boolean` - **optional** (default is false)
> *   `status`: `danger` | `warning` | `success` | `notification` — **optional** (background color of tooltip)

## Default Tooltip

> `caption: string;` - caption for the tooltip

<Canvas />

## Bottom Tooltip

> `bottom: boolean;` - positioning tooltip at the bottom

<Canvas />

## Tooltip Position

> *   `position`: `left` | `right` - you can align the tooltip to the right side or to the left side of your child element

<Canvas />

## Tooltip Status

> *   `status`: `danger` | `warning` | `success` | `notification`

<Canvas />

## Tooltip with other Components

<Canvas />
