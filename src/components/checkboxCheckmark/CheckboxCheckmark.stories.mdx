import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { CheckboxCheckmark, CheckboxCheckmarkGroup } from ".";
import { FlexContainer } from "../container";

<Meta title="Forms/CheckboxCheckmark" component={CheckboxCheckmark} />

# CheckboxCheckmark

### CheckboxCheckmark properties

|      properties | required | type                 | description                                          |
| --------------: | :------: | :------------------- | :--------------------------------------------------- |
|       labelText |  false   | string               | Checkbox label                                       |
|    withoutLabel |  false   | boolean              | Checkbox without label with larger clickable surface |
|    tooltipProps |  false   | ExtendedTooltipProps | Props for customising the tooltip                    |

### Default

<Canvas>
  <Story name="simple">
    <FlexContainer
      style={{
        padding: "3rem",
      }}
    >
      <CheckboxCheckmark id="checkbox-1" labelText="GNUI Checkbox" />
    </FlexContainer>
  </Story>
</Canvas>

### With double checkmark

<Canvas>
  <Story name="double">
    <FlexContainer
      style={{
        padding: "3rem",
      }}
    >
      <CheckboxCheckmark id="checkbox-2" labelText="GNUI Checkbox" double />
    </FlexContainer>
  </Story>
</Canvas>


### withoutLabel

<Canvas>
  <Story name="withoutLabel">
    <FlexContainer
      style={{
        padding: "3rem",
      }}
    >
      <CheckboxCheckmark withoutLabel id="checkbox-5" />
    </FlexContainer>
  </Story>
</Canvas>

# CheckboxGroup

### CheckboxGroup properties

| properties | required | type                | description |
| ---------: | :------: | :------------------ | :---------- |
|   children |  false   | `ReactElement<any>` |             |

<Canvas>
  <Story name="group">
      {() => {
        const [value, setValue] = React.useState({
          0: false,
          1: false,
          2: false,
        });
        const handleCheckbox = (index) =>
          setValue({ ...value, [index]: !value[index] });
        const labels = ["New York", "San Francisco", "Los Angeles"];
        const names = ["checkbox-1", "checkbox-2", "checkbox-3"];
        return (
          <FlexContainer
            just
            style={{
              padding: "3rem",
            }}
          >
            <CheckboxGroup name="gnui-checkbox-group">
              {names.map((names, index) => (
                <CheckboxCheckmark
                  id={names[index]}
                  onChange={() => handleCheckbox(index)}
                  labelText={labels[index]}
                />
              ))}
            </CheckboxGroup>
          </FlexContainer>
        );
      }}
  </Story>
</Canvas>
