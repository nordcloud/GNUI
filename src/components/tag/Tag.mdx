import { Meta, Story, Canvas } from "@storybook/blocks";
import * as TagStories from "./Tag.stories";

<Meta of={TagStories} />

# Tag

- `color`: `string` – _optional_, Default Tag background color is `theme.colors.lights[2]` [list of available colors](?path=/docs/foundation-styleguide--colors)
- `colorText`: `string` – _optional_, Default Tag text color is `theme.colors.primary` [list of available colors](?path=/docs/foundation-styleguide--colors)
- `text`: `string` — _optional_, Tag content
- `icon`: `string` — optional, icon to be placed inside the tag [list of available icons](?path=/docs/components-svgicon--ui)
- `onClick`: `function` — _optional_
- `showClose`: `boolean` — _optional_, show close button
- `isTransparent`: `boolean` — _optional_, this prop makes the tag background transparent

<Canvas>
  <Story of={TagStories.TagDefault} />
</Canvas>

## Colored Tag with close button

> `color` props can be used in two ways: as an action colors build with `theme` (`accent`, `danger`, `success`, `warning`, `notification`, `primary` "red", `redInverse`, `pink`, `purple`, `indigo`, `blue`, `blueInverse`, `grey`, `greyInverse`, `cyan`, `teal`, `green`, `greenInverse`, `lightGreen`, `yellow`, `orange`,`orangeInverse`)

<Canvas>
  <Story of={TagStories.CloseButton} />
</Canvas>

## Colored Tag with icons

> By default, if you use custom colors text color will be `snowWhite` you can change text color using additional props `textColor`

<Canvas>
  <Story of={TagStories.TagWithIcon} />
</Canvas>
