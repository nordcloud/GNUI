import styled from "styled-components";
import {
  Meta,
  Story,
  Canvas,
  Title,
  Description,
} from "@storybook/addon-docs/blocks";
import { StickyBar } from "./";
import { Spacer } from "../spacer";
import { Box } from "../box";
import theme from "../../theme";

<Meta title="Components/StickyBar" component={StickyBar} />

<Title>Sticky Bar</Title>

<Description>
  Element acts like it is relatively positioned until it is scrolled to a
  certain threshold point (0px from the top of the viewport), after which it
  becomes fixed.
</Description>

## Props

> - `children`: `React Node`

## Default

<Canvas>
  <Story name="default">
    <div
      style={{
        maxHeight: "400px",
        overflowY: "scroll",
      }}
    >
      <StickyBar>
        Some description text
        <a href="#">link text</a>
      </StickyBar>
      <Spacer height="5rem" />
      <Box border="default">Sample content</Box>
      <Spacer height="5rem" />
      <Box border="disabled">Sample content</Box>
      <Spacer height="5rem" />
      <Box border="danger">Sample content</Box>
      <div
        style={{
          minHeight: "100vh",
        }}
      />
    </div>
  </Story>
</Canvas>
