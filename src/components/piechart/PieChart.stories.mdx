import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Text } from "../text";
import { FlexContainer } from "../container";
import { PieChart } from ".";
import { SVGIcon } from "../svgicon";

<Meta title="Components/PieChart" component={PieChart} />

# PieChart

## PieChart properties

|  properties | required | type                                                                                       | description              |
| ----------: | :------: | :----------------------------------------------------------------------------------------- | :----------------------- |
|        size |   true   | number                                                                                     | width and height in `px` |
|    progress |   true   | number                                                                                     |                          |
| strokeWidth |   true   | number                                                                                     | chart stroke width       |
|       color |  false   | <code>danger</code>, <code>warning</code>, <code>success</code>, <code>notification</code> |                          |
|    children |  false   | <code>`ReactNode`</code>                                                                   | elements inside chart    |

## Default

<Canvas>
  <Story name="default">
    <PieChart progress={33} size={96} strokeWidth={15}>
      <Text className="chartValue" size="lg" tag="span" weight="medium">
        33%
      </Text>
      <Text className="chartCaption" size="xs" tag="span">
        progress
      </Text>
    </PieChart>
  </Story>
</Canvas>

## Stroke Width

<Canvas>
  <Story name="strokeWidth">
    <FlexContainer justifyContent="space-evenly">
      <PieChart progress={33} size={96} strokeWidth={5}>
        <Text className="chartValue" size="lg" tag="span" weight="medium">
          33%
        </Text>
        <Text className="chartCaption" size="xs" tag="span">
          progress
        </Text>
      </PieChart>
      <PieChart progress={33} size={96} strokeWidth={15}>
        <Text className="chartValue" size="lg" tag="span" weight="medium">
          33%
        </Text>
        <Text className="chartCaption" size="xs" tag="span">
          progress
        </Text>
      </PieChart>
    </FlexContainer>
  </Story>
</Canvas>

## Color

- You can change color using `color` props: `danger` | `warning` | `success` | `notification`;

<Canvas>
  <Story name="color">
    <Flex justifyContent="space-evenly">
      <PieChart progress={13} size={96} strokeWidth={15} color="danger">
        <Text className="chartValue" size="lg" tag="span" weight="medium">
          13%
        </Text>
        <Text className="chartCaption" size="xs" tag="span">
          progress
        </Text>
      </PieChart>
      <PieChart progress={26} size={96} strokeWidth={15} color="warning">
        <Text className="chartValue" size="lg" tag="span" weight="medium">
          26%
        </Text>
        <Text className="chartCaption" size="xs" tag="span">
          progress
        </Text>
      </PieChart>
      <PieChart progress={65} size={96} strokeWidth={15} color="success">
        <Text className="chartValue" size="lg" tag="span" weight="medium">
          65%
        </Text>
        <Text className="chartCaption" size="xs" tag="span">
          progress
        </Text>
      </PieChart>
      <PieChart progress={75} size={96} strokeWidth={15} color="notification">
        <Text className="chartValue" size="lg" tag="span" weight="medium">
          75%
        </Text>
        <Text className="chartCaption" size="xs" tag="span">
          progress
        </Text>
      </PieChart>
      <PieChart progress={100} size={96} strokeWidth={15} color="success">
        <SVGIcon name="success" color="success" size="md" />
        <Text className="chartCaption" size="xs" tag="span">
          progress
        </Text>
      </PieChart>
    </FlexContainer>
  </Story>
</Canvas>
