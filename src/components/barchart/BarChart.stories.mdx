import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { BarChart } from "./";
import theme from "../../theme";

<Meta title="Components/BarChart" component={BarChart} />

# BarChart

> BarChart is used to compare at least two values.

## BarChart properties

| properties | required | type      | description              |
| ---------: | :------: | :-------- | :----------------------- |
|     values |   true   | BarItem[] |                          |
|     height |  false   | string    | default is 1.5rem (24px) |

## BarItem properties

| properties | required | type   | description |
| ---------: | :------: | :----- | :---------- |
|      value |   true   | number |             |
|      color |   true   | string |             |

## Default

<Preview>
  <Story name="default">
    {() => {
      const values = [
        {
          value: 4629.75,
          color: theme.color.support.red,
        },
        {
          value: 2006.28,
          color: theme.color.support.blue,
        },
        {
          value: 599.66,
          color: theme.color.support.orange,
        },
      ];
      return <BarChart values={values} />;
    }}
  </Story>
</Preview>
