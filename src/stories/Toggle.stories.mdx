import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { Toggle } from "../components/toggle";
import { useState } from "react";

<Meta title="Components/Toggle" component={Toggle} />

# Toggle

#### Props

- `name`: `string` **required** 
- `labelText`: `string` **required**
- `toggleValue`: `boolean` — _optional_
- `size`: `string` — _optional_ - small-sized SelectButton
- `status`: `string` — _optional_ - color indicator status for SelectButton
- `handleChange`: `() => boolean` — _optional_ - React `setState` to handle toggleValue change

### Default

<Preview>
  <Story name="default">
    <Toggle labelText="Default" name="default" />
  </Story>
</Preview>

### Small

> - `size`: `string` — [`small`]

<Preview>
  <Story name="small">
    <Toggle size="small" labelText="Small" name="small" />
  </Story>
</Preview>

### Color Status

> - `status`: `string` — [`success`, `danger`, `warning`, `notification`]

<Preview>
  <Story name="status">
  <div>
    <p>
    <Toggle status="success" labelText="Success!" name="success" />
    </p>
    <p>
    <Toggle status="danger" labelText="Danger" name="danger" />
    </p>
    <p>
    <Toggle status="warning" labelText="Warning" name="warning" />
    </p>
    <p>
    <Toggle status="notification" labelText="Notification" name="notification" />
    </p>
  </div>
  </Story>
</Preview>
