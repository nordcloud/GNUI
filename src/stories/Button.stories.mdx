import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { Button } from "../components/button";
import { Container } from "../components/container";

<Meta title="Components/Button" component={Button} />

# Button

> Primary button is default as well.

<Preview>
  <Story name="primary/default">
    <Button>Primary button</Button>
  </Story>
</Preview>

## Props

- `outline`: `boolean` — _optional_
- `size`: `string` — _optional_
- `color`: `string` — _optional_
- `severity`: `string` — _optional_
- `disabled`: `boolean` — _optional_

### outline

> `outline`: `boolean`

<Preview>
  <Story name="outline/secondary">
    <Button outline>Primary button</Button>
  </Story>
</Preview>

### color

> `color`: `string` — can be used in two ways: as an action colors build with `theme` (`accent`, `danger`, `success`, `warning`, `notification`) or just in HEX/named color formats according to [X11 Colors](https://en.wikipedia.org/wiki/X11_color_names)

<Preview>
  <Story name="color">
    <Container>
      <p><Button color="accent">Accent color</Button></p>
      <p><Button color="danger">Danger color</Button></p>
      <p><Button color="success">Success color</Button></p>
      <p><Button color="warning">Warning color</Button></p>
      <p><Button color="notification">Notification color</Button></p>
      <p><Button color="#FF1493">HEX color</Button></p>
    </Container>
  </Story>
</Preview>

### severity

> `severity`: `string` — [`high`, `medium`, `low`]

<Preview>
  <Story name="severity">
    <Container>
      <p><Button severity="high">High severity</Button></p>
      <p><Button severity="medium">Medium severity</Button></p>
      <p><Button severity="low">Low severity</Button></p>
    </Container>
  </Story>
</Preview>

### disabled

> `disabled`: `boolean`

<Preview>
  <Story name="disabled">
    <Container>
      <p><Button disabled>Disabled button</Button></p>
      <p><Button outline disabled>
        Disabled button
      </Button></p>
      <p><Button severity="low" disabled>
        Disabled button
      </Button></p>
      <p><Button color="warning" disabled>
        Disabled button
      </Button></p>
      <p><Button color="#FF1493" disabled>
        Disabled button
      </Button></p>
    </Container>
  </Story>
</Preview>
