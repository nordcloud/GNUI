import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { MultipleSelect } from "../components/selectbutton";
import { useState } from "react";

<Meta title="Components/SelectButton" component={MultipleSelect} />

# SelectButton

#### Props

- `list`: `array` — **required** and containes the following **required** props: `name`: `string`, `value`: `string`, `labelText`: `string` and the following _optional_: `id`, `className`
- `size`: `string` — _optional_ - small-sized SelectButton
- `status`: `string` — _optional_ - color indicator status for SelectButton

### Default

<Preview>
  <Story name="default">
  {() => {
    const list = [
    { name: "button1", value: "button1", labelText: "Button 1"},
    { name: "button2", value: "button2", labelText: "Button 2"},
    { name: "button3", value: "button3", labelText: "Button 3"}
    ];
    return(
    <MultipleSelect list={list} />
    );
  }
  }
  </Story>
</Preview>

### Small

> - `size`: `string` — [`small`]

<Preview>
  <Story name="small">
  {() => {
    const list = [
    { name: "button1", value: "button1", labelText: "Button 1"},
    { name: "button2", value: "button2", labelText: "Button 2"},
    { name: "button3", value: "button3", labelText: "Button 3"}
    ];
    return(
    <MultipleSelect size="small" list={list} />
    );
  }
  }
  </Story>
</Preview>

### Color Status

> - `status`: `string` — [`success`, `danger`, `warning`, `notification`]

<Preview>
  <Story name="status">
  {() => {
    const list = [
    { name: "button1", value: "button1", labelText: "Button 1"},
    { name: "button2", value: "button2", labelText: "Button 2"},
    { name: "button3", value: "button3", labelText: "Button 3"}
    ];
    return(
    <div>
      <p>Success</p>
      <MultipleSelect status="success" list={list} />
      <p>Danger</p>
      <MultipleSelect status="danger" list={list} />
      <p>Warning</p>
      <MultipleSelect status="warning" list={list} />
      <p>Notification</p>
      <MultipleSelect status="notification" list={list} />
    </div>
    );
  }
  }
  </Story>
</Preview>
