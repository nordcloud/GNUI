import { useState } from "react";
import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { Button } from "../components/button";
import { Filter } from "../components/filter";
import { Input } from "../components/input";
import { Container, Row, Col } from "../index";

<Meta title="Components/Filter" component={Filter} />

# Filter

> Please take a look at props to understand correctly how `Filter` works.

### Props

- `label`: string - **required**
- `accompanyingComponent`: JSX.Element - _optional_
- `isFilterOpen`: boolean - _optional_

<Preview>
  <Story name="filter">
    <Filter label="Filter">
      <p>Some content</p>
    </Filter>
  </Story>
</Preview>

## Filter with component

> Filter with a input component

<Preview>
  <Story name="filter with component">
    <Filter
      label="Filter"
      isFilterOpen={false}
      accompanyingComponent={
        <Input name="input" type="search" placeholder="Search something" />
      }
    >
      <Container>
        <Row>
          <Col>
            <Input name="input" placeholder="filter something" />
          </Col>
          <Col>
            <Input name="input" placeholder="filter something else" />
          </Col>
        </Row>
        <Row>
          <Col>
            <Button secondary>Apply settings</Button>
          </Col>
        </Row>
      </Container>
    </Filter>
  </Story>
</Preview>
