import { PathName } from "../../svgicon/paths";
type Props = {
    caption: string;
    icon: PathName;
    expanded: boolean;
};
export declare function Trigger({ expanded, icon, caption }: Props): import("react/jsx-runtime").JSX.Element;
export {};
