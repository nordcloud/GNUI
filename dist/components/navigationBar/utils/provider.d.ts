import * as React from "react";
import { PopoverConfig } from "../types";
type Props = {
    children: React.ReactNode;
    popoverConfig?: PopoverConfig;
};
export declare function NavigationBarProvider({ children, popoverConfig, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
